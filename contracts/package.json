{
  "name": "contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "starknet-compile src/*.cairo --output build/",
    "deploy": "bash scripts/deploy.sh",
    "deploy:testnet": "bash scripts/deploy_testnet.sh",
    "add-levels": "bash scripts/add-levels.sh"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "jest": "^29.0.0"
  }
}